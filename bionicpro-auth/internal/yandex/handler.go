package yandex

// Intentionally left without HTTP handlers.
// Yandex consent/profile endpoints are implemented in internal/httpapi/server.go
// because the service already uses session-based authentication there.
